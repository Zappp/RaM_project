{
  "name": "ram-app",
  "version": "0.1.0",
  "private": true,
  "description": "Rick & Morty app with React, Supabase, and GraphQL",
  "scripts": {
    "install:api": "cd api && deno cache --node-modules-dir --lock deno.lock --reload supabase/functions/graphql/index.ts",
    "dev:api": "cd api && deno task dev",
    "build:api": "cd api && supabase functions deploy graphql",
    "test:api": "cd api && deno task test",
    "check:api": "cd api && deno task check"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
