{
  "name": "ram-app",
  "version": "0.1.0",
  "private": true,
  "description": "Rick & Morty app with React, Supabase, and GraphQL",
  "scripts": {
    "api": "cd api && deno task cache && deno task dev",
    "frontend": "cd frontend && pnpm install && pnpm dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
